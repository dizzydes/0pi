service: mcp
runtime: python311
entrypoint: gunicorn -w 2 -k uvicorn.workers.UvicornWorker -b :$PORT mcp_http.app:app

instance_class: F2

env_variables:
  BACKEND_BASE_URL: "https://YOUR_PROJECT_ID.uc.r.appspot.com"

handlers:
- url: /.*
  script: auto

